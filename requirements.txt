# Solar Carbon Cost Calculator - Data Pipeline Dependencies
#
# Core (required for compile_data.py with fallback data):
#   No dependencies needed — uses only Python stdlib
#
# For fetching live data from APIs:
#   No extra dependencies — uses urllib (stdlib)
#
# For processing Cambium Excel workbooks:
pandas>=2.0
openpyxl>=3.1
